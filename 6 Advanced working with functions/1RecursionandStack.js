// console.log(pow(2,3))
// No such function, you need to write it by yourself.

let tasks = `Sum all numbers till the given one
Calculate factorial
Fibonacci numbers
Output a single-linked list
Output a single-linked list in the reverse order 
    Recursion: if (list.next) {printReverseList(list.next);}
    Iterative: arr
`;

console.log(tasks);
